/**index.wxss**/
.content{
   width: 100%;
   height:450;
   background-color: rgb(240, 240, 240);
   bottom: 0;
   display: flex;
   flex-direction: column;
}
.user_top{
   height: 170rpx;
}
.user_infor{
   background-color: white;
   width: 100%;
   height: 120rpx;
   /* border: 1rpx solid pink; */
   padding-top: 50rpx;
   /* display
   block vertical
   flex horizontal
    */
   display: flex;
   /* flex-directio
   row：从左到右的水平方向为主轴
   row-reverse：从右到左的水平方向为主轴
   column：从上到下的垂直方向为主轴
   column-reverse：从下到上的垂直方向为主轴
 */
   flex-direction: row;
  /* justify-content 
  flex-start：主轴起点对齐(默认值)
  flex-end：主轴结束点对齐
  center：在主轴中居中对齐
  space-between：两端对齐，除了两端的子元素分别靠向两端的容器之外，
   其他子元素之间 的间隔都相等
  space-around：每个子元素之间的距离相等，两端的子元素距离容器的距离也和
   其它子元素之间的距离相同
   */
   justify-content:space-around;

   /* align-items
      stretch 填充整个容器(默认值)
      flex-start 侧轴的起点对齐 （这里我们手动设置下子 view 的高度，
      来看的明显一些）
      flex-end 侧轴的终点对齐
      center 在侧轴中居中对齐
      baseline 以子元素的第一行文字对齐
    */
    align-items:stretch;
}
.head_name{
   display: flex;
   flex-direction: row;
   /* border: 1rpx solid green; */
   height: 120rpx;
}
.head_image{
   /* display: flex; */
   width:120rpx;
   height: 120rpx;
   background-color: white;
   border-radius: 60rpx;
}

.user_name{
   display: flex;
   justify-content: right;
   font-size: 25rpx;
   padding-left: 15rpx;
   margin-top: 15rpx;
   height: 30rpx;
   /* border: 1rpx solid gold; */
   text-align: left;
}

.loginAcount{
   border: 1rpx solid rgb(91, 104, 116);
   color: rgb(105, 113, 123);
   height: 80rpx;
   width: 200rpx;
   font-size: 35rpx;
   border-radius: 25rpx;
   display: flex;
   justify-content: center;
   align-items:Center;
}

.urser_bottom{
   height: 150rpx;
   background-color: white;
   /* border: 1rpx solid rebeccapurple; */
   display: flex;
   flex-direction: row;
   justify-content: space-between;
   border-bottom: 1rpx solid rgb(211, 211, 211);
}

.found_count{
   display: flex;
   flex-direction: column;
   justify-content: center;
   align-items:Center;
   /* border: 1rpx solid red; */
   width: 50%;
   font-size: 25rpx;
}
.lost_count{
   display: flex;
   flex-direction: column;
   justify-content: center;
   align-items:Center;
   /* border: 1rpx solid green; */
   width: 50%;
   font-size: 25rpx;
}
.fans_count{
   display: flex;
   flex-direction: column;
   justify-content: center;
   align-items:Center;
   /* border: 1rpx solid blue; */
   width: 33.333%;
   font-size: 25rpx;
}
